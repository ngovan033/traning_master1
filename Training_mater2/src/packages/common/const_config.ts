import { ColumnOptions } from "@/types";

export const CONST_CONFIG_STT: ColumnOptions = {
  // Cột STT
  caption: "STT",
  dataField: "STT",
  visible: true,
  width: 80,
  alignment: "center",
};
